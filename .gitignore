# Persistent runtime-данные DSH (сессии, чаты, credentials, node_modules плагинов,
# кэш Chromium) — это состояние контейнера, а не часть проекта, и весит сотни МБ
data/

# Секреты (API-ключи, токены gh/glab) — реальные значения только тут,
# в docker-compose.yml должны быть только ${VAR} ссылки. Шаблон: .env.example
.env

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
