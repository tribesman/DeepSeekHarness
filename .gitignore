# Persistent runtime-данные DSH (сессии, чаты, credentials, node_modules плагинов,
# кэш Chromium) — это состояние контейнера, а не часть проекта, и весит сотни МБ
data/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Секреты (API-ключи, токены gh/glab) и личные пути хоста — реальные значения
# только тут, в git не попадают. Шаблон: docker-compose.override.example.yml
docker-compose.override.yml
